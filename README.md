## Getting Started

Since this is a simple version of a Task manager, it runs locally from TaskAppFX.java.

## Requirements

JDK 17.0.19
Openjjfx-21.0.11

## Folder Structure

The workspace contains two folders by default, where:

- `src`: the folder to maintain sources
